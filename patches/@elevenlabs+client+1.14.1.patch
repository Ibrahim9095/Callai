diff --git a/node_modules/@elevenlabs/client/dist/BaseConversation.js b/node_modules/@elevenlabs/client/dist/BaseConversation.js
index 13c5815..7276834 100644
--- a/node_modules/@elevenlabs/client/dist/BaseConversation.js
+++ b/node_modules/@elevenlabs/client/dist/BaseConversation.js
@@ -266,8 +266,13 @@ export class BaseConversation {
         }
     }
     handleErrorEvent(event) {
-        const errorType = event.error_event.error_type;
-        const message = event.error_event.message || event.error_event.reason || "Unknown error";
+        const errorEvent = event && event.error_event;
+        if (!errorEvent) {
+            this.onError("Server error: Unknown error", { errorType: undefined });
+            return;
+        }
+        const errorType = errorEvent.error_type;
+        const message = errorEvent.message || errorEvent.reason || "Unknown error";
         if (errorType === "max_duration_exceeded") {
             void this.endSessionWithDetails({
                 reason: "error",
@@ -280,9 +285,9 @@ export class BaseConversation {
         }
         this.onError(`Server error: ${message}`, {
             errorType,
-            code: event.error_event.code,
-            debugMessage: event.error_event.debug_message,
-            details: event.error_event.details,
+            code: errorEvent.code,
+            debugMessage: errorEvent.debug_message,
+            details: errorEvent.details,
         });
     }
     onMessage = async (parsedEvent) => {
